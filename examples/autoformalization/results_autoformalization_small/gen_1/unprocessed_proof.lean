import data.real.basic
import data.real.nnreal
import Mathlib.Tactic

-- EVOLVE-BLOCK-START
theorem abelian_group {G : Type _} [Group G] (a b : G) :
  (a * b = b * a) →
  let H := Subgroup.closure ({a, b} : Set G) in
  ∀ x y : G, x ∈ H → y ∈ H → x * y = y * x
:= by
  classical
  intro hcomm
  let H := Subgroup.closure ({a, b} : Set G)
  -- In a group, the subgroup generated by commuting elements is abelian.
  -- We provide a minimal, tactic-assisted proof sketch using existing lemmas.
  have hAb : IsAbelian H := by
    -- use the standard fact that the closure of a commuting set is abelian
    simpa using Subgroup.closure_isAbelian_of_commute (S := ({a, b} : Set G)) hcomm
  intro x y hx hy
  have hx' : (⟨x, hx⟩ : H) * ⟨y, hy⟩ = ⟨y, hy⟩ * ⟨x, hx⟩ := by
    simpa using hAb.mul_comm ⟨x, hx⟩ ⟨y, hy⟩
  simpa using hx'
-- EVOLVE-BLOCK-END